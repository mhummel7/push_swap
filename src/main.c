#include "push_swap.h"

int main(int argc, char **argv)
{
	t_stack_node *a;
	t_stack_node *b;

	a = NULL;
	b = NULL;
	if (argc == 1 || !argv[1][0])
		return (0);
	if(argc == 2)
		handle_one_arg(argv[1])

}